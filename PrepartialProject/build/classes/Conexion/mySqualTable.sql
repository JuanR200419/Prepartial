CREATE TABLE Student (
    identification VARCHAR(50) PRIMARY KEY NOT NULL,
    name VARCHAR(50) NOT NULL,
    degree VARCHAR(50) NOT NULL,
    subject VARCHAR(50) NOT NULL,
    age INT NOT NULL
);


